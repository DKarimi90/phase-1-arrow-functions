// defining a function expression; 
const divide = function() {
    return 2000/100; 
}

// defining an arrow function; 
const square = (a = 2) => (a * a); 

// defining another arrow function; 
const add = (a, b) => (a + b); 
